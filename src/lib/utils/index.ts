/**
 * Utility re-exports
 */

export { formatPrice, formatPhone, truncateText, slugify } from './format';
export {
  contactFormSchema,
  validateContactForm,
  type ContactFormData,
} from './validation';
